package com.seismic.crm.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.seismic.crm.model.ProductSubType;

public interface ProductSubTypeRepository extends
		JpaRepository<ProductSubType, Long> {
		ProductSubType findByProductSubType(String productSubType);
}
